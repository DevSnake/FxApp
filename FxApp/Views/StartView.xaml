<Page
    x:Class="FxApp.Views.StartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView DisplayMode="CompactOverlay" x:Name="MySplitView"
               IsPaneOpen="False" CompactPaneLength="50" OpenPaneLength="200">
        <SplitView.Pane>
            <StackPanel>
                <Grid Background="#FFE3E6E6">
                    <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" 
                        Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click"/>
                </Grid>
                


            </StackPanel>
        </SplitView.Pane>
        
        <SplitView.Content>
            <Grid>
                <Grid Background="#FFE3E6E6" VerticalAlignment="Top" Height="50">
                    <TextBlock Text="SOFT-FX Test App" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                </Grid>
            </Grid>
        </SplitView.Content>
    </SplitView>
    <!--<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
    </Grid>-->
</Page>
